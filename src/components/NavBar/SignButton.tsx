import { UserIcon } from "../ui/Icons";

const SignButton = () => {
  return (
    <button className='flex' type='button'>
      <UserIcon />
      Log In
    </button>
  );
};

export default SignButton;
